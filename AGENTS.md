Read all Markdown files in `.agents/skills/`.

# UI components

- Always use `@mui/material` components ONLY
- For icons, prefer `@mui/material-icons`, but you may also use `lucide-react`
  as a fallback
