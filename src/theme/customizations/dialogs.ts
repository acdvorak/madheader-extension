import type { Components, Theme } from '@mui/material/styles';

export const dialogCustomizations: Components<Theme> = {
  /*
  MuiDialog: {
    styleOverrides: {
      paper: ({ theme }) => {
        const palette = (theme.vars || theme).palette;

        return {
          // width: 'calc(100vw - 4em)',
          // height: 'calc(100vh - 4em)',
          // maxWidth: 'none',
          // maxHeight: 'none',
          margin: theme.spacing(2),
          display: 'flex',
          flexDirection: 'column',
          outline: '1px solid',
          outlineColor: palette.grey[500],
        };
      },
    },
  },

  MuiDialogTitle: {
    styleOverrides: {
      root: ({ theme }) => {
        const palette = (theme.vars || theme).palette;

        return {
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'space-between',
          gap: theme.spacing(2),
          paddingTop: theme.spacing(1),
          paddingBottom: theme.spacing(1),
          borderBottom: '1px solid currentColor',
          borderBottomColor: palette.grey[500],
        };
      },
    },
  },

  MuiDialogContent: {
    styleOverrides: {
      root: ({ theme }) => {
        const palette = (theme.vars || theme).palette;

        return {
          display: 'flex',
          flex: 1,
          minHeight: 0,
          padding: 0,
          backgroundColor: palette.background.paper,
          ...theme.applyStyles('dark', { backgroundColor: palette.grey[800] }),
        };
      },
    },
  },

  MuiDialogActions: {
    styleOverrides: {
      root: ({ theme }) => {
        const palette = (theme.vars || theme).palette;

        return {
          padding: theme.spacing(1),
          borderTop: '1px solid',
          borderTopColor: palette.divider,
        };
      },
    },
  },
  */
};
